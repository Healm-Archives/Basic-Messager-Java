spring:
        application:
                name: messanging
        # profiles:
        #         active:
                        # - demo                        
        datasource:
                # url: jdbc:mysql://localhost:3306/spring_jpa?createDatabaseIfNotExist=true
                # username: root
                # password: admin

                # url: jdbc:postgresql://db-postgresql:5432/postgres
                url: jdbc:postgresql://localhost:5432/postgres
                # ?createDatabaseIfNotExist=true
                username: myuser
                password: myuser

                # driver-class-name: com.mysql.cj.jdbc.Driver

        jpa:
                hibernate:
                        ddl-auto: update
                database: postgresql
                show-sql: true
                open-in-view: true

server:
        port: 8080